# Kept

A small, honest habit tracker for iPhone and iPad. Paid once ($0.99), no ads, no accounts,
no subscription, no analytics. Everything stays on the device.

- Add a habit with a name, icon, and color.
- Tap the circle each day. Streaks count consecutive days and survive until the day after a miss.
- Open a habit for current streak, best streak, total days, last-30-day rate, and a tappable
  month calendar to fix missed check-ins.
- A week-at-a-glance dot strip on every row and a "3 of 5 done" progress bar for the day.
- Home-screen widgets with interactive check-off, and lock-screen widgets (iOS 17).
- Rest-day schedules: weekdays only, or any days you choose. Skipped days never break a streak.
- Daily reminders that skip rest days and days already done.
- A twelve-month heatmap on every habit.
- Siri and Shortcuts: "Mark drink water done in Kept."
- Reorder or swipe-to-delete from the list.
- Export everything as plain JSON from the About screen. Your data is yours.

## Requirements

- macOS with Xcode 15 or newer (iOS 17 SDK).
- An Apple Developer Program membership to ship to the App Store.
- [XcodeGen](https://github.com/yonaskolb/XcodeGen) to produce the `.xcodeproj` from `project.yml`.

## Build

```sh
brew install xcodegen
xcodegen generate
open Kept.xcodeproj
```

Then select your team under **Signing & Capabilities** and run on a simulator or device.

If you would rather not install XcodeGen: create a new iOS App project in Xcode named `Kept`
(SwiftUI, Swift), delete the template `ContentView.swift` and `KeptApp.swift`, and drag the
`Kept/` folder's contents into the project. Add `KeptTests/` to a new Unit Testing Bundle target.
Set `ITSAppUsesNonExemptEncryption` to `NO` in the target's Info tab.

## Test

```sh
xcodebuild test -project Kept.xcodeproj -scheme Kept \
  -destination 'platform=iOS Simulator,name=iPhone 15'
```

The tests cover streak math (including month, year, and leap-day boundaries), `DayKey` encoding,
and JSON persistence round-trips.

## Ship

Every push runs the build and unit tests on a GitHub-hosted Mac (see the CI workflow). To
upload a signed build to App Store Connect without owning a Mac, follow
[docs/RELEASE.md](docs/RELEASE.md) and run the **Release to App Store Connect** workflow.

See [docs/APP_STORE_SUBMISSION.md](docs/APP_STORE_SUBMISSION.md) for the step-by-step checklist,
[docs/APP_STORE_LISTING.md](docs/APP_STORE_LISTING.md) for ready-to-paste store copy, and
[docs/PRIVACY_POLICY.md](docs/PRIVACY_POLICY.md) for the policy page Apple requires you to host.

## Layout

```
project.yml                 XcodeGen spec (targets, Info.plist keys, bundle ID)
Kept/
  KeptApp.swift             @main entry point
  KeptShortcuts.swift       Siri / Shortcuts phrases
  PrivacyInfo.xcprivacy     Privacy manifest (no tracking, no data collected)
  Assets.xcassets/          App icon (generated by scripts/make_icon.py) and accent color
  Models/                   DayKey, Habit                      (shared with the widget)
  Logic/                    StreakCalculator, ReminderPlanner  (shared with the widget)
  Store/                    HabitStore, App Group JSON file    (shared with the widget)
  Shared/                   App Group id, AppIntents           (shared with the widget)
  Services/                 ReminderScheduler (UserNotifications)
  Views/                    SwiftUI screens
KeptWidget/                 WidgetKit extension: home-screen and lock-screen widgets
KeptTests/                  XCTest unit tests
scripts/make_icon.py        Regenerates the 1024x1024 icon with no dependencies
docs/                       Submission runbook, listing copy, privacy policy
```
